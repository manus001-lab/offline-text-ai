# ブラウザ内Qwen推論の実装メモ

## 実装方式

WebLLMはWebGPUを利用して、言語モデルの推論をブラウザ内で実行するライブラリです。`MLCEngine`を初期化した後、OpenAI互換の`chat.completions.create` APIで応答を生成でき、ストリーミング出力にも対応します。[1]

## 重要な制約

モデル本体の重みはライブラリに同梱されません。WebLLMの公式ドキュメントでは、初回モデルロード時にダウンロードが必要で、キャッシュがない初回実行では相当の時間がかかる可能性があると明記されています。[1]

したがって、推論そのものはブラウザのWebGPUでローカル実行できますが、まったくネットワークに接続しない状態で初めてQwenを動かすには、モデル重みを事前配布・事前キャッシュする必要があります。本プロジェクトでは、初回取得後は端末側のキャッシュを再利用する構成とし、会話文の生成時にクラウドAPIへ送る実装は置きません。

## 参照

[1]: https://webllm.mlc.ai/docs/user/basic_usage.html "WebLLM Basic Usage"
